## 1. 子页面迁移准备

- [x] 1.1 为 COA 控制台创建 `pages/coa-controller/index.html` 页面骨架，并接入主站共享样式与返回首页入口。
- [x] 1.2 将现有 `COAController.html` 的结构和交互节点整理为适合子页面模板的内容分区与说明文案。

## 2. 控制台逻辑与配置改造

- [x] 2.1 新建页面专属脚本文件并迁移原内联脚本，保持状态刷新、动作发送、账号别名缓存和冷却限制逻辑可用。
- [x] 2.2 将 `GIST_ID` 从硬编码常量改为页面输入项与本地缓存配置，并在读取/写入 Gist 前统一校验 Token 与 Gist ID。

## 3. 主站入口与视觉统一

- [x] 3.1 在 `assets/js/site-data.js` 中新增 COA 控制台卡片配置，使首页可以展示并进入该子页面。
- [x] 3.2 在 `assets/css/site.css` 中补充控制台页所需的表单、状态卡和操作区样式，使其与现有作品集风格一致。

## 4. 验证与兼容收尾

- [x] 4.1 验证首页卡片、`/pages/coa-controller/` 独立访问、返回首页导航和本地配置回填行为。
- [x] 4.2 验证状态刷新与固定动作发送在指定 Gist ID 下仍能正确构造请求，并处理缺失配置或请求失败时的提示。
